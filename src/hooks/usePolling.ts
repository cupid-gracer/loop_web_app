import { useEffect } from "react"

const usePolling = () => {
  /*const pair  = []
  const { startPolling, stopPolling } = pair

  useEffect(() => {
    startPolling?.(3000)
  }, [startPolling])

  useEffect(() => {
    return () => stopPolling?.()
  }, [stopPolling])*/
}

export default usePolling
