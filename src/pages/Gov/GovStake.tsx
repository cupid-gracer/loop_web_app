// import { LOOP } from "../../constants"
// import useHash from "../../libs/useHash"
import Page from "../../components/Page"
// import { Type } from "../Stake"
// import { MenuKey } from "../Gov"
// import FarmStakeFarm from "../../forms/FarmStakeForm"
// import {useProtocol} from "../../data/contract/protocol";
// import RewardList from "./RewardList"
// import useDashboard, { StatsNetwork } from "../../statistics/useDashboard"

const GovStake = () => {
  // const { getToken } = useProtocol()
  // const { network } = useDashboard(StatsNetwork.COMBINE)
  // const token = getToken(LOOP)
  // const { hash: type } = useHash<Type>(Type.STAKE)
  // const tab = { tabs: [Type.STAKE, Type.UNSTAKE], current: type }

  return (
    <Page >
    {/*  {type && (
        <FarmStakeFarm type={type} token={token} tab={tab} key={type} gov />
      )}*/}
    </Page>
  )
}

export default GovStake
